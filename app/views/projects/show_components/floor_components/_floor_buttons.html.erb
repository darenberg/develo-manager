
<% @project.floors.each do |floor| %>
    <button class="col-sm-5 m-2" type="button" data-action="click->maps#changeFloor" id="floor<%= floor.number %>"> <%=floor.number%> </button>
<% end %>

<%= button_to "+", project_floors_path(@project), method: :post, params: { floor: { number: 1 } } %>

<%# add delete %>
<%# add negative and 0 %>
