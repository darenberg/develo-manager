<div class="d-flex row align-items-center border-bottom">
  <div class="d-flex col">
  <h4><strong>Floor Plans</strong></h4>
  </div>
  <div class="d-flex col">
    <%  Plan::STAGES.each do |stage| %>
      <button data-maps-target="stageBtn" type="button" class="plan-btn" data-action="click->maps#changePlan" id="plan-<%= stage %>"> <%= stage.capitalize %> </button>
    <% end %>
  </div>
</div>
